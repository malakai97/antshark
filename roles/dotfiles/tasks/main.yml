- name: download dotfiles
  git:
    accept_hostkey: true
    repo: "{{dotfiles_git}}"
    dest: "/home/{{user}}/dotfiles"

- name: remove .git dir
  file:
    path: "/home/{{user}}/dotfiles/.git"
    state: absent

- name: cp out dotfiles
  copy:
    remote_src: true
    src: "/home/{{user}}/dotfiles/"
    dest: "/home/{{user}}/"

- name: remote emtpy dotfiles dir
  file:
    path: "/home/{{user}}/dotfiles"
    state: absent
